/*package sayTheSpire.ui.mod;

import com.megacrit.cardcrawl.helpers.controller.CInputActionSet;


public class InputAction {

    public enum ActionType {
        UP, DOWN, LEFT, RIGHT, INSPECT_UP, INSPECT_DOWN, INSPECT_LEFT, INSPECT_RIGHT, SELECT, CANCEL, TOP_PANEL, PROCEED, PEEK, VIEW_DECK, VIEW_EXHAUST, MAP, SETTINGS, DRAW_PILE, DISCARD_PILE, MOD
    };

    public ActionType actionType;

    
    public InputAction(ActionType type) {
        this.actionType = type;
    }

    public Boolean isJustPressed() {
        switch(this.actionType) {
            case UP:
            return CInputActionSet.up.isJustPressed() || CInputActionSet.altUp.isJustPressed();
                        case DOWN:
            return CInputActionSet.down.isJustPressed() || CInputActionSet.altDown.isJustPressed();
            case LEFT:
            return CInputActionSet.left.isJustPressed() || CInputActionSet.altLeft.isJustPressed();
            case RIGHT:
            return CInputActionSet.up.isJustPressed() || CInputActionSet.altRight.isJustPressed();
            case INSPECT_UP:
            return CInputActionSet.inspectUp.isJustPressed();
            case INSPECT_DOWN:
            return CInputActionSet.inspectDown.isJustPressed();
            case INSPECT_LEFT:
return CInputActionSet.inspectLeft.isJustPressed();            
case INSPECT_RIGHT:
return CInputActionSet.inspectRight.isJustPressed();
            case SELECT:
            return CInputActionSet.select.isJustPressed();
            case CANCEL:
            return CInputActionSet.cancel.isJustPressed();
            case TOP_PANEL:
            return CInputActionSet.topPanel.isJustPressed();
            case PROCEED:
            return CInputActionSet.proceed.isJustPressed();
            case PEEK:
            return CInputActionSet.peek.isJustPressed();
            case VIEW_DECK:
            return CInputActionSet.pageLeftViewDeck.isJustPressed();
            case VIEW_EXHAUST:
            return CInputActionSet.pageRightViewExhaust.isJustPressed();
            case MAP:
            return CInputActionSet.map.isJustPressed();
            case SETTINGS:
            return CInputActionSet.settings.isJustPressed();
            case DRAW_PILE:
            return CInputActionSet.drawPile.isJustPressed();
            case DISCARD_PILE:
            return CInputActionSet.discardPile.isJustPressed();
            default:
            return null;
        }
    }
}
*/